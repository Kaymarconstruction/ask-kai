Ask Kai – Quote Generator html, body { height: 100%; margin: 0; padding: 0; display: flex; flex-direction: column; } #kaiReply { max-height: 60vh; overflow-y: auto; } .blinking-cursor { display: inline-block; width: 10px; height: 1em; background-color: #7AC142; margin-left: 4px; animation: blink 1s steps(2, start) infinite; } @keyframes blink { 0%, 100% { opacity: 0; } 50% { opacity: 1; } } 

Ask Kai – Quote Generator Back to Chat 

Please describe your job in detail. Be very specific – location, materials, sizes, etc. 

Submit 

<!-- Kai Profile --> <div class="max-w-2xl w-full bg-white rounded-2xl shadow-lg p-6 mt-8"> <img src="https://i.imgur.com/uYWtV9r.png" alt="Kai Marlow" class="mx-auto mb-4 w-48 h-72 object-cover bg-white p-2 rounded-2xl shadow" /> <h2 class="text-2xl font-bold text-center text-green-600 mb-4">Who is Kai?</h2> <p class="text-gray-700 text-center"> With 20+ years across carpentry and building in Australia, Kai gives sharp, clear advice backed by code, tools, and know-how.<br> He’s your mate on-site, online. Ask him anything. </p> </div> © 2025 Kaymar Construction | Powered by Ask Kai 

Instagram TikTok Facebook YouTube Threads 

Built with coding support from Jack Cooper

const user = sessionStorage.getItem('askkaiUser'); if (!user) window.location.href = 'signin.html'; const isMark = user === 'mark@kaymarconstruction.com'; let promptCount = parseInt(localStorage.getItem('promptCount') || '0'); const MAX_FREE_PROMPTS = 10; const messages = [{ role: "system", content: "You are Kai, a senior estimator and builder. Always ask for detailed project info, estimate all parts of structure: stumps, bearers, joists, decking, fasteners. Output in markdown dot-point format." }]; document.getElementById('submitBtn').addEventListener('click', async () => { const askInput = document.getElementById('askInput'); const kaiReply = document.getElementById('kaiReply'); const userQuestion = askInput.value.trim(); if (!userQuestion) return; if (!isMark && promptCount >= MAX_FREE_PROMPTS) { alert("You’ve hit your free limit. Time to upgrade."); window.location.href = 'upgrade.html'; return; } messages.push({ role: "user", content: userQuestion }); const qaContainer = document.createElement('div'); qaContainer.className = 'mb-6'; const userText = document.createElement('p'); userText.className = 'text-right text-blue-700 font-semibold mb-2'; userText.textContent = `You: ${userQuestion}`; qaContainer.appendChild(userText); const kaiText = document.createElement('p'); kaiText.className = 'text-left text-green-600 font-semibold whitespace-pre-wrap'; const cursor = document.createElement('span'); cursor.className = 'blinking-cursor'; kaiText.textContent = "Kai is thinking..."; kaiText.appendChild(cursor); qaContainer.appendChild(kaiText); kaiReply.appendChild(qaContainer); kaiReply.scrollTop = kaiReply.scrollHeight; askInput.value = ""; try { const response = await fetch('https://askkai-backend-clean.onrender.com/quote', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ messages }) }); const data = await response.json(); const finalText = data.reply || "Kai’s stumped. Try again shortly."; kaiText.textContent = ""; kaiText.appendChild(cursor); let i = 0; function typeNextChar() { if (i 
